Issue #38 handoff

Changed `.github/workflows/check-drift.yml` so `pull_request` events always run
the `check` job. This removes the PR path filter that prevented unrelated PRs
from producing the required status check, while leaving the existing push path
filter in place to avoid redundant branch-push runs.

Updated the workflow header comment to document that PRs run unconditionally for
required-check support and pushes remain path-filtered.

Validation:

- `node scripts/check-targets-drift.mjs` exits 0 on the clean tree.
- Temporary README generated-block drift exits 1 with
  `README.md :: targets block :: drifted from data/targets.json`.
- The temporary README drift was restored, and the clean drift check exits 0
  again.
