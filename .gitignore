# Local screenshots — not program assets (those live in Assets/ and are tracked)
截屏*.png
Screenshot*.png
Screen Shot*.png

# macOS
.DS_Store

# Local-only setup notes / tutorials — never published
local-setup-notes/

# Internal collaboration notes — local only, never published
docs-internal/

# Local agent handoff / task artifacts — never published
.handoff-*.md
.issue-*.json
